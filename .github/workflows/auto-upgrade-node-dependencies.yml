name: Auto-upgrade Node Dependencies

on:
    workflow_dispatch:
    schedule:
        - cron: '0 0 * * *'

env:
    CARGO_TERM_COLOR: always
    AWS_REGION: us-east-1

jobs:
    upgrade:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4
              with:
                  token: ${{ secrets.LOG_SERVICE_UPGRADES_TOKEN }}

            - uses: pnpm/action-setup@v3
              name: Install pnpm
              with:
                  version: 9
                  run_install: false

            - name: Get pnpm store directory
              shell: bash
              run: |
                  echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

            - name: ⚡ Cache pnpm
              uses: actions/cache@v4
              with:
                  path: ${{ env.STORE_PATH }}
                  key: ${{ matrix.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
                  restore-keys: |
                      ${{ matrix.os }}-pnpm-store-

            - name: pnpm install
              run: pnpm install

            - name: Upgrade
              id: upgrade
              shell: bash
              run: |
                  echo "Updating general dependencies"
                  pnpm update
                  echo "Updating SST dependencies"
                  pnpm sst upgrade --stage upgrade
                  pnpm install --no-frozen-lockfile
                  pnpm update
                  echo "Formatting package.json"
                  pnpm prettier --write package.json
                  if [ -n "$(git status --porcelain)" ]; then
                      echo "Found upgrades"
                      echo "upgraded=true" >> "$GITHUB_OUTPUT"
                  else
                      echo "No upgrades"
                      echo "upgraded=false" >> "$GITHUB_OUTPUT"
                  fi

            - name: Validate
              if: ${{ steps.upgrade.outputs.upgraded == 'true'}}
              shell: bash
              run: |
                  pnpm validate

            - name: Commit
              if: ${{ steps.upgrade.outputs.upgraded == 'true'}}
              shell: bash
              run: |
                  git config user.name "Braden Steffaniak"
                  git config user.email "BradenSteffaniak+bot@gmail.com"
                  git add .
                  git commit --message "Upgrade Node Dependencies"
                  echo "Pushing upgrades"
                  git push origin master
